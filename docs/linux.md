# Linux
*Update/Upgrade*
sudp apt update
sudo apt upgrade

*Grant access*
sudo chown -R $USER <path>

## NVM
*Install*
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash

*Check global dirs*
nvm which current